{% set name = "biofilm" %}
{% set version = "0.0.101" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/biofilm-{{ version }}.tar.gz
  sha256: c28a367fb28eb3fe85c19ac250e50155269aecf09b10d3a3b042d451a83fc957

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  host:
    - pip
    - python >=3.8
  run:
    - python >=3.8
    - auto-sklearn >=0.15
    - dirtyopts >=0.0.18
    - eden-kernel >=0.3.1348
    - lmz >=0.1.12
    - numpy >=1.22.0
    - scikit-learn >=0.24.2
    - structout >=0.1.44
    - ubergauss >=0.0.43
    - pynisher <=0.7

test:
  imports:
    - biofilm
    - biofilm.algo

about:
  home: https://github.com/smautner/biofilm
  summary: another opts parser
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - smautner
